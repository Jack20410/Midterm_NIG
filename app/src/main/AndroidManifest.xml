<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- Legacy storage flag for backward compatibility -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:requestLegacyExternalStorage="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyGallery"
        tools:targetApi="34">

        <!-- MainActivity as the Launcher -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".LoginActivity" android:exported="true" />
        <activity android:name=".RegisterActivity" android:exported="true" />
        <activity android:name=".Image.ImagesDisplayActivity" />
        <activity android:name=".OnlineActivity" />
        <activity android:name=".Utilities.RecycleBinActivity" />
        <activity android:name=".Image.ImageInspectActivity" />
        <activity android:name=".Album.OfflineAlbumActivity" />
        <activity android:name=".Album.OfflineAlbumInspect"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".Slideshow.SlideshowActivity"
            android:parentActivityName=".Album.OfflineAlbumInspect"/>
        <activity android:name=".Favorite.FavoriteActivity" />
        <activity android:name=".Utilities.SearchActivity"
            android:label="Search"
            android:exported="true" />
    </application>

</manifest>
